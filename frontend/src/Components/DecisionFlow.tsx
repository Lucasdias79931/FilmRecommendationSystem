import type { DecisionNode } from "../types/movies_types";

interface NodeProps{
    node: DecisionNode;
}

const DecisionFlow = ({node}: NodeProps)=>{
    const []
    return (
        <div>
            
        </div>
    )
}

export default DecisionFlow;